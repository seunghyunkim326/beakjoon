# 소수 : 자연수 M이상 N이하의 자연수 중 소수인 것을 골라 소수의 합과 최솟값 출력   소수가 없을 경우 -1 출력
M = int(input())
N = int(input())
prime_number = []
# M ~ N 사이 자연수 중 소수 판별 후 소수 리스트에 삽입
for i in range(M, N+1):
    count = 0
    # 1을 생각하지 않아서 조건문으로 다시 시도
    if i == 1:
        pass
    else:
        for j in range(1, i+1):
            if i % j == 0:
                count += 1
        if count < 3:
            prime_number.append(i)
summation = 0
# 소수 리스트의 합을 구하는 코드
for k in range(len(prime_number)):
    summation += prime_number[k]
# 소수가 있으면 합과 최솟값을 출력하고 소수가 없으면 -1을 출력하는 코드
if len(prime_number) != 0:
    print(summation)
    print(prime_number[0])
elif len(prime_number) == 0:
    print(-1)

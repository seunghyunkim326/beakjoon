# 배수와 약수 : 0 0이 입력되면 끝남, 그 전까지 2개 숫자 입력받음, 첫 숫자가 두 번째 숫자의 약수면 factor, 배수면 multiple, 둘 다 아니라면 neither
# 0 0이 나오기 전까지 반복으로 입력받음
while True:
    fir_num, sec_num = map(int, input().split())
    if fir_num == 0 and sec_num == 0:
        break
    # 앞의 숫자가 뒷 숫자의 약수인지 배수인지 판별하는 코드
    if fir_num > sec_num:
        if fir_num % sec_num == 0:
            print("multiple")
        else:
            print("neither")
    elif fir_num < sec_num:
        if sec_num % fir_num == 0:
            print("factor")
        else:
            print("neither")
